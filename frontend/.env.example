# Optional: override API base URL (default is /api, using Vite proxy in dev)
VITE_API_BASE_URL=/api

# Optional: Vite dev proxy target (used by vite.config.js)
# Local without Docker: http://localhost:8000
# Docker compose network: http://nginx
VITE_PROXY_TARGET=http://localhost:8000
